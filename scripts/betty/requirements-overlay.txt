# Packages absent from NVIDIA TensorFlow 25.02. Dependencies are deliberately
# supplied by the immutable base image; install this file with --no-deps.
lightgbm==4.6.0
venn-abers==1.5.3
# Plotting packages are absent from the NGC training image but are required by
# the locked analysis/aggregation environment.
contourpy==1.3.3
cycler==0.12.1
fonttools==4.62.1
kiwisolver==1.5.0
matplotlib==3.10.8
pillow==12.1.1
# The definitive train_y.pkl was serialized by pandas 3 and is not readable by
# the image's pandas 2.2 compatibility unpickler.
pandas==3.0.1
